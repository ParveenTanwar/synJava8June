Demo 4 
Lab1 - Create and print instnace of java.util.Date
	compile with -profile compact1 option
       modify java file to include java.sql.Date
       	compile with -profile compact1 option -> error
	compile with -profile compact2 option -> success

	java -verbose:class Lab1 (CLassLoaders) 
	
	Jdeps 
		jdeps Lab1.class 
		jdeps .
	
		-s, -v
	running this on any existing class
		
Lab 2 = Date and Time API
	Accept year, month and date from user and create Date Object
	
Lab 3 - Optional demo
	Write Lab3 to test the code and check the return values
	class Lab3Helper {
		private List<String> l1 =  Arrays.asList("Vaishali","Simran", "Vasanti");
	
		public Optional<Integer> find(String str) //throws Exception
		{
			for (int i = 0; i < l1.size(); i++) {
				if (l1.get(i).equals(str))
				return Optional.of(i);
		}
		return Optional.empty();
		}
		}


---------------------------------------------	
Demo 3- 
Asgn 1 - (demo3)
	Create a list of 10 emps, 
		1. modify all employees to have  salary = 1000
		2. modify employees where salary > 2000, salary += 10% of salary
		3. Filter (Predicate)
			Show records satisfying that condition
			
---------------------------------------------			

Lab1.java 
Lab 1 - 
  Consumer, Function, Operator,Predicate, Supplier

    1. int sqr(int)
    2. double sqr(double)
    3. int add(int , int)
    4. double divide(String, String)
    5. void print(String )
    6. int random()
    7. double getSalary(Emp)
    8. String concat(String, String)
    9. void process(Dept d) -> insert a record in table
    10. bool checkOdd(int)

Lab 2 -Lab2.java single line vs multiline
	impact of return on both
		
		BinaryOperator<String> concat = (i, j) -> i + j;
		 concat = (i,j)->{ return i+j; };
Lab 3 -Lab3.java  Variable declaration in lambda
	New variable -> read/write
	Variable outside scope
		Final or effectively final
	Static Calls to functions
	list.forEach(System.out::println);
